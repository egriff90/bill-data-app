[build]
builder = "nixpacks"

[variables]
NODE_ENV = "production"

# Main backend API service
[services.backend]
startCommand = "cd packages/backend && node dist/index.js"

# Cron job service
[services.cron]
startCommand = "cd packages/backend && node dist/sync/cron-sync.js"
